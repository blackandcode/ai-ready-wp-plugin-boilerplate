meta {
  name: Hello World REST Endpoint
  type: http
  seq: 2
}

get {
  url: {{apiRoot}}/hello
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.message: isString
  res.body.status: eq ok
  res.body.version: isString
  res.body.timestamp: isString
}
